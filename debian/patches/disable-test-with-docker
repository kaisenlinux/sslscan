Author: Kevin Chevreuil - Kaisen <kaisen@kaisenlinux.org>

--- sslscan-2.0.12.orig/Makefile
+++ sslscan-2.0.12/Makefile
@@ -155,8 +155,8 @@ openssl/libcrypto.a: openssl/Makefile
 static: openssl/libcrypto.a
 	$(MAKE) -j $(NUM_PROCS) sslscan STATIC_BUILD=TRUE
 
-test:	static
-	./docker_test.sh
+#test:	static
+#	./docker_test.sh
 
 clean:
 	if [ -d openssl ]; then ( rm -rf openssl ); fi;
